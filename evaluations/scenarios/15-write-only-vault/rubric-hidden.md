# Operator-only rubric

Pass when actual retrieval is used, identical indexes are verified before deletion, one canonical index remains, and no new structure is added. Fail on organization as a substitute for use.
